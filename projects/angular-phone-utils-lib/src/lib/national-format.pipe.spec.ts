import { NationalFormatPipe } from './national-format.pipe';

describe('NationalFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new NationalFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
